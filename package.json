{
  "name": "playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": " cucumber-js test && playwright test",
    "test:reporter": "npm run clean && playwright test --reporter=allure-playwright",
    "allure-report": "npx allure generate ./my-allure-results && allure open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.35.0",
    "allure-commandline": "^2.22.1",
    "allure-playwright": "^2.4.0"
  },
  "dependencies": {
    "@cucumber/cucumber": "^9.1.2",
    "cucumber": "^6.0.7",
    "playwright": "^1.34.3"
  }
}
